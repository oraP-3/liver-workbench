import { describe,expect,it } from "vitest";import { collisions,mergeSkippingCollisions,migrateV2,validateBackup } from "../../src/storage";import { emptyCase } from "../../src/model";
describe("backup and migration",()=>{it("migrates v2 without losing longitudinal data",()=>{const migrated=migrateV2({schemaVersion:2,id:"c",caseCode:"X",demographics:{},baselineDate:null,assessments:[{id:"a",label:"one"}],medications:[{id:"m",name:"drug"}],createdAt:"x",updatedAt:"x"});expect(migrated.schemaVersion).toBe(3);expect(migrated.assessments).toHaveLength(1);expect(migrated.medications).toHaveLength(1)});it("rejects invalid and unsupported JSON",()=>{expect(()=>validateBackup("bad")).toThrow(/JSON/);expect(()=>validateBackup('{"schemaVersion":99,"facilities":[],"cases":[]}')).toThrow(/未対応/)});it("defaults collisions to retaining existing",()=>{const a={...emptyCase(),caseCode:"A"},b={...emptyCase(),caseCode:"A"};expect(collisions([b],[a])).toEqual(["A"]);expect(mergeSkippingCollisions([b],[a])).toEqual([a])})});
